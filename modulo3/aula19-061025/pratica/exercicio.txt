Crie duas tabelas no seu banco: (usuarios e postagens);
Usuarios: terá atributo (colunas): id, primeiro_nome, sobrenome, email e senha;
Postagens: terá atributos (colunas): id, titulo, postagens e id_autor;
O id_usuarios será um id existente de usuarios;
Faça 5 inserções de valores em cada tabela.